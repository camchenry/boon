version: 2

jobs:
    test:
        docker:
            - image: circleci/rust:latest
        steps:
            - checkout
            - run:
                name: Test
                command: cargo test
    build:
        docker:
            - image: circleci/rust:latest

        steps:
            - checkout
            - restore_cache:
                key: project-cache
            - run:
                name: Stable Build
                command: |
                    rustc --version --verbose
                    cargo --version --verbose
                    cargo build
            - save_cache:
                key: project-cache
                paths:
                    - "~/.cargo"
                    - "./target"
