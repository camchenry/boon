[build]
# If true, build will not be allowed when DEBUG is true
debug_halt = true

# Which folders and files to exclude when building. (Anything partially containing these too).
ignore_list = [
    ".git",
    "releases",
    "play.bat",
    ".gitattributes",
    "screenshots",
    ".love",
    ".exe"
]

[project]
authors = "Love2D guy"
description = "Your description here."
email = "email@example.com"
package_name = "Package 1"
project_title = "My Game"
project_url = "http://www.example.com/"
project_uti = "public.item"
project_version = "v1.0"
